<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ light: the system's wallpaper-derived neutral, so the window
     behind the first frame matches the app's own surface colour. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/system_neutral1_10" />
</layer-list>
